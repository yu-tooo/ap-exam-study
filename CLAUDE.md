# CLAUDE.md

## プロジェクト概要

このプロジェクトはユーザーがIPAの応用情報技術者試験（Applied Information Technology Engineer Examination）を学ぶためのプロジェクトである。**ソフトウェアプロジェクトではない。** - ビルド、テスト、アプリケーションコードも存在せず、ユーザーが作成するテキストファイル、エージェントが変換するマークダウンファイルのみがある（格納場所は「コンテンツ構成」に記載）。

## コンテンツ構成

- `メモ/` - ユーザーが作成したテキストファイルを格納する。
- `情報システム開発/` - エージェントがテキストファイルから変換したマークダウンファイルを格納する。
- `プロジェクトマネジメント/` - エージェントがテキストファイルから変換したマークダウンファイルを格納する。
- `サービスマネジメント/` - エージェントがテキストファイルから変換したマークダウンファイルを格納する。
- `システム監査/` - エージェントがテキストファイルから変換したマークダウンファイルを格納する。
- `アルゴリズム/` - エージェントがテキストファイルから変換したマークダウンファイルを格納する。
- `基礎理論/` - エージェントがテキストファイルから変換したマークダウンファイルを格納する。
- `コンピュータシステム/` - エージェントがテキストファイルから変換したマークダウンファイルを格納する。
- `データベース/` - エージェントがテキストファイルから変換したマークダウンファイルを格納する。
- `ネットワーク/` - エージェントがテキストファイルから変換したマークダウンファイルを格納する。
- `情報セキュリティ/` - エージェントがテキストファイルから変換したマークダウンファイルを格納する。
- `ストラテジ/` - エージェントがテキストファイルから変換したマークダウンファイルを格納する。
- `応用数学/` - エージェントがテキストファイルから変換したマークダウンファイルを格納する。
- `その他/` - エージェントがテキストファイルから変換したマークダウンファイルを格納する。
- `変換ログ.md` - メモのマークダウン変換履歴を記録するログファイル。

## 運用

1. ユーザーが`メモ/`配下に学習用のテキストファイルを格納する。
2. ユーザーの指示によりエージェントがテキストファイルをマークダウンファイルに変換し、適切なディレクトリ配下に格納する。1つのテキストファイルから、セクション単位で複数のマークダウンファイルが生成されうる。
3. 必要に応じて、ユーザーもしくはエージェントがマークダウンファイルに追記もしくは変更する。
4. マークダウンファイルの作成・変更時は`create-markdown`スキルを使用する。
5. メモ内に `{...}` で囲まれた記述がある場合は、ユーザーからエージェントへの質問・依頼として扱い、変換時に回答・補足情報を該当箇所に組み込む（詳細は`create-markdown`スキルの「情報追加のポリシー」を参照）。

## 許可される操作

- ファイル・ディレクトリの読み取り（Read、Glob、Grep等のツール）。
- `メモ/`以外のディレクトリへのマークダウンファイルの追加・編集。

## 禁止事項

- エージェントが`メモ/`配下のファイルを追加・修正・削除すること（読み取りは許可）。
- 日本語以外の言語を使用すること。
- gitの操作をすること。
- シェルコマンドの実行。
